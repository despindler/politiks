Read every Markdown file directly under `.agents/` and every relevant Markdown file under `.agents/skills/`, including the complete files rather than excerpts. Use `.agents/CONTEXT.md` to build a mental model of the Politiks product and its project-specific constraints, `.agents/CODEX.md` for the repository's engineering discipline, and `.agents/PLAN.md` for an overview of what we have done so far.
