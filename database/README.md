# Research database

This directory will contain the authoritative SQLite `schema.sql` and the generated, ignored `parliament.sqlite` research database.

The database must always be reproducible from committed schema, raw source files, and the import notebook. Do not treat a committed binary database as the source of truth.
