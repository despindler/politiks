# Raw source data

This directory will contain substantively unmodified responses from official parliamentary sources, retrieval manifests, and source documentation.

Acquisition code must preserve response boundaries, URLs, query parameters, UTC retrieval times, HTTP status, content type, byte counts, SHA256 checksums, and retry/error state. Partial downloads use the ignored `.part` suffix. Full snapshot policy and commands will be implemented in Milestone 1.
