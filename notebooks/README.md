# Research notebooks

The first notebook will be `01_import_source_data.ipynb`. It will recreate the SQLite database from `database/schema.sql` and import local raw responses without network access.

Every code cell must have a concise Markdown explanation immediately above it and bounded, informative output where appropriate.
